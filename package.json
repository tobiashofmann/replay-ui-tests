{
  "name": "replay-recorded-ui-tests",
  "version": "1.0.0",
  "description": "Replay UI tests recorded using the Chrome test recorder. Uses the SAP SFLIGHT demo Fiori Elements app as app under test.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ui5",
    "fiori",
    "elements",
    "replay",
    "puppeteer",
    "ui",
    "testing"
  ],
  "author": "Tobias Hofmann",
  "license": "Apache-2.0",
  "dependencies": {
    "@puppeteer/replay": "^3.1.1"
  },
  "devDependencies": {
    "puppeteer": "^23.4.1"
  }
}
